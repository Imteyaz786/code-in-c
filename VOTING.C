#include<sdio.h>
#include<conio.>
int main()
{
int age;
printf("Program to check Eligible for vote or not");
prinf("Enter you age :");
if(age<18)
{
printf("You are not Eligible for vote");
}
if(age>=18)
{
printf("You are Eligible for vote");
}
getch();
}